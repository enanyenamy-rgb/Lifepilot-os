name: ai_phone
description: "AI Phone - The ultimate global smartphone specs database & secure local used marketplace with multilingual support."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0

flutter:
  uses-material-design: true
  generate: true
